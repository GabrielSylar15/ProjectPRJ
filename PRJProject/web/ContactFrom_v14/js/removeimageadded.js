function removeImage(){
    var images = document.querySelectorAll(".wrapper-imageadded img");
    document.querySelectorAll("removeImageAdded").forEach(element => {
        element.onclick = function(){
            
        }
    });
}